/* Stub: DOOM's generic source (i_system.c) includes <unistd.h> but,
 * in this build's configuration (see config.h: HAVE_UNISTD_H is
 * undef), never actually calls anything declared by it. Empty on
 * purpose. */
#ifndef DOOM_UNISTD_H
#define DOOM_UNISTD_H
#endif

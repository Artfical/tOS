/* Stub: included by i_input.c/i_video.c but nothing from it is
 * actually called in this build (all real file I/O goes through
 * kernel/lib/stdio.h's fopen/fread/etc instead). Empty on purpose. */
#ifndef DOOM_FCNTL_H
#define DOOM_FCNTL_H
#endif
